# different ways to use python
# taked about IDEs
# talked about defining a function, ran a fun one
# then matplotlib and pd.read_csv

# numeric types: int, float (has a limit, special = +/- inf, nan), string variables.

# variable assignment: evaluates rhs, associates lhs with resulting value

# str values are sequences. use [indexed value] to access each ch in a str

# lists: sequeces of variables that are ordered by index

# dictionaries: basically a look up table. eg:
trial = {'doc1': 'document 1', 'doc2': 'document 2'}
# >>> trial 
# {'doc1': 'document 1', 'doc2': 'document 2'}
# >>> trial['doc1']
# 'document 1'

